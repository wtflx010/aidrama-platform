# ── dgx-spark-cluster-dashboard 配置模板 ─────────────────────────────
# 复制为 .env 后按需修改：  cp .env.example .env
# 中控读取：  node --env-file-if-exists=.env apps/server/src/index.ts

# 中控监听端口（默认 8890）
# PORT=8890

# 中控时序库路径（默认 <项目>/data/central.db）
# DB=./data/central.db

# 要拉取的 agent 列表：<节点id>=<agent HTTP 地址>，逗号分隔
# 把占位符换成你的真实值（见 VARIABLES.md）：
AGENTS=<HOST_A>=http://<IP_MGMT_A>:9100,<HOST_B>=http://<IP_MGMT_B>:9100

# 前端构建产物目录（默认 apps/dashboard/dist）
# DIST=./apps/dashboard/dist
