"""分镜语法常量（单一来源，消除跨模块硬编码重复）。

集中管理剧本/分镜生成与提示词增强两条链路共用的枚举与判定规则，避免
shot_beats / storyboard_parser / 模板等各写一份导致规则漂移（2026-08-31）。

- SHOT_TYPES / CAMERAS：景别与运镜枚举（前端右侧面板、分镜生成、归一化校验共用）；
- EMOTIONS：分镜整体与对白情绪枚举（须与前端/模板枚举一致）。
"""
from __future__ import annotations

# 景别枚举（与前端 StudioRightPanel、分镜生成模板保持一致）
SHOT_TYPES = ("远景", "全景", "中景", "近景", "特写")
# 运镜枚举
CAMERAS = ("固定", "推", "拉", "摇", "移", "跟")
# 情绪枚举（分镜整体氛围 / 对白 emotion）
EMOTIONS = (
    "愤怒", "悲伤", "平静", "欢快", "紧张",
    "温馨", "恐惧", "史诗", "冷漠", "震惊",
)
